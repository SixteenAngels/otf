{
  "build": {
    "builder": "dockerfile",
    "buildCommand": "docker build -t concert-backend ./backend",
    "watchPatterns": ["backend/**/*"]
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
